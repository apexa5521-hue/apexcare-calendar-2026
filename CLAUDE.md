# تقويم عيادات أبكس كير 2026 — سياق المشروع

## نظرة عامة
تقويم تفاعلي عربي (RTL) لعيادات أبكس كير — تسنيني وجلدية، فرعين (بريدة وعنيزة). نظام دخول بحسابات أدوار، ملاحظات مجدولة بين الأقسام، مزامنة مع Google Sheets.

## الملفات
- **`index.html`** — الملف الرئيسي الكامل (HTML+CSS+JS في ملف واحد)، يُنشر على GitHub Pages
- **`Code.gs`** — كود Google Apps Script الخلفي (Backend) — بهذا الاسم بالضبط في المستودع (نسخة العمل المحلية أحياناً باسم `apexcare_calendar.gs`، انسخ محتواها لـ Code.gs عند الرفع)
- **Repo الصحيح:** `https://github.com/apexa5521-hue/apexcare-calendar-2026` (تنبيه: `apexa5521-hue/Calendar` مستودع مختلف/قديم — لا تستخدمه، تم الخلط بينهما سابقاً)
- **Live URL:** `https://apexa5521-hue.github.io/apexcare-calendar-2026/`
- **الشيت الأساسي للبيانات:** اسمه `Notes` بالإنتاج الفعلي (الكود يدعم أيضاً `الملاحظات` كاسم بديل ويقرأ من الاثنين، لكن الكتابة تفضّل الشيت الموجود فعلياً)

## معمارية النظام

### حسابات الدخول (6 حسابات بأرقام سرية)
| الحساب | الدور (key) | PIN | الصلاحية |
|---|---|---|---|
| المدير التنفيذي | `ceo` | 1234 | يرى **كل** الملاحظات (seesAll:true) |
| التسويق | `marketing` | 2345 | يرى ما أُرسل له/منه فقط |
| الموارد البشرية | `hr` | 3456 | يرى ما أُرسل له/منه فقط |
| العمليات | `ops` | 4567 | يرى ما أُرسل له/منه فقط |
| الجودة | `quality` | 5678 | يرى ما أُرسل له/منه فقط |
| عام | `general` | 0000 | عرض فقط (canWrite:false) |

هذه معرّفة في JS كـ `PINS` و `ROLE_META` object.

### قواعد رؤية الملاحظات
- CEO يرى كل شيء دائماً
- غيره يرى: ما أرسله + ما وُجِّه إليه + العام (`to_role:'all'`) + المشترك (`shared:'yes'`)
- عند كتابة ملاحظة: المرسِل لا يظهر في قائمة المستقبِلين (لا يرسل لنفسه)

### تخزين البيانات — نظام ثلاثي الطبقات
1. **`localNotes`** (متغير JS في الذاكرة) — أثناء الجلسة
2. **`localStorage`** (مفتاح `apx_notes_2026`) — يبقى بعد تحديث الصفحة/بين الجلسات
3. **Google Sheets** — المصدر الحقيقي المشترك بين كل الأجهزة، عبر JSONP (Apps Script)

**مهم:** الشيت هو "مصدر الحقيقة" — `fetchFromSheet()` تستبدل المحلي بما في الشيت، مع الاحتفاظ بالملاحظات المعلّقة (`pendingIds`) التي لم تُؤكَّد بعد من الشيت.

### أعمدة شيت Google Sheets
```
id | date | title | text | link | from_role | to_role | color | shared | ts
```
- الشيت الأساسي: `الملاحظات` (يقرأ أيضاً من `Notes` و `CalendarEvents` للتوافق مع نسخ قديمة، ويدمج النتائج)
- `date` يجب أن يكون بصيغة `YYYY-MM-DD` — الكود فيه دالة `normalizeDate()` (JS) و`fmtDate()` (GAS) لتحويل أي صيغة تاريخ (مثل `Thu Jan 08 2026...`) للصيغة الصحيحة، لأن Google Sheets أحياناً يخزّن التاريخ كـ Date object فيرجعه بصيغة طويلة

### دوال Apps Script الرئيسية (apexcare_calendar.gs)
- `doGet(e)` — نقطة الدخول، يدعم JSONP عبر `callback` parameter
- `getSheet()` / `getWriteSheet()` / `getAllSheets()` — إدارة الشيتات المتعددة
- `getAllNotes(p)` — قراءة مع فلترة حسب `viewer` (الدور)
- `saveNote(p)` — حفظ، مع تحقق من التكرار بالـ id، وتلوين الصف حسب `from_role`
- `deleteNote(p)` / `shareNote(p)`
- `getColMap(headers)` — يتعرف ديناميكياً على أسماء الأعمدة (عربي/إنجليزي)
- `ensureLinkCol()` / `ensureTitleColumn()` / `setupAllColumns()` — دوال صيانة يدوية (تُشغَّل من قائمة Run في Apps Script) لإضافة أعمدة ناقصة للشيتات القديمة دون حذف بيانات

**تحذير معروف:** لا تستخدم `for...of` في GAS (لا يدعمها Apps Script القديم) — استخدم `for` عادية. ولا تضع `\n` حرفي داخل single-quoted string (يكسر الـ parser) — استخدم `\\n`.

### دوال JS الرئيسية (index.html)
- تسلسل الدخول: `doLogin()` → `lsLoad()` → `rebuildFromLocal()` (عرض فوري من الكاش) → `fetchFromSheet()` (تحديث من الشيت) → `initNewFeatures()`
- `fetchFromSheet()` / `gasRead()` / `gasWrite()` — التواصل مع GAS عبر JSONP (وليس `fetch` مع `no-cors`، لأن Apps Script يعمل redirect 302 يكسر no-cors)
- `submitNote()` — إرسال ملاحظة جديدة، مع `pendingIds` retry logic
- `renderCell(date)` / `renderAllCells()` — رسم الـ chips على خلايا التقويم
- `openNoteDetail(id)` — نافذة تفاصيل الملاحظة (عنوان + نص + رابط + حالة + ردود)
- `setNoteStatus()` / `NOTE_STATUS` — حالات: `new`, `read`, `done`, `rejected`
- `sendReply()` / `renderThread()` — نظام ردود Thread على كل ملاحظة
- `showArchive()` / `checkArchive()` — أرشفة تلقائية (منجزة بعد 7 أيام، أو مرفوضة فوراً) — **لا تؤرشف أبداً ملاحظة تاريخها مستقبلي**
- `showAlerts()` / `updateAlertsBadge()` — تنبيهات اليوم والغد
- `showReport()` — تقرير أسبوعي للمدير فقط (إحصائيات + قابل للطباعة)

## مميزات مبنية بالفعل
1. تقويم 2026م كامل (12 شهر) + تقويم هجري مبني بالحساب
2. أيام صحية عالمية + إجازات رسمية + إجازات تعليم (مضمّنة كبيانات ثابتة في الكود)
3. Anchor تلقائي على الشهر الحالي عند الدخول + تمييز اليوم الحالي
4. عنوان + نص + رابط لكل ملاحظة (الرابط يظهر كـ HYPERLINK في الشيت وكـ popup في التقويم)
5. حالة الملاحظة (جديدة/مقروءة/منجزة/مرفوضة) مع أزرار تغيير الحالة
6. نظام ردود Thread على كل ملاحظة
7. أرشيف منفصل مع استرجاع
8. تقرير أسبوعي PDF للمدير
9. طباعة PDF محسّنة (A3 أفقي 4×3)
10. Favicon SVG مضمّن

## مشاكل تم حلها سابقاً (خبرة تراكمية — لا تكرر هذه الأخطاء)
- **الملاحظات تختفي بعد ثوانٍ:** السبب كان `processNotes` تستبدل كل شيء بما يرجع من الشيت حتى لو رجع فارغاً. الحل: `localStorage` + `pendingIds` + عدم مسح المحلي أبداً إذا الشيت رجع فارغاً بينما المحلي فيه بيانات
- **الملاحظة تختفي بعد Logout/Login:** كان الكود يمسح `localStorage` عند logout. الحل: لا تمسح `localStorage` أبداً عند logout، فقط امسح متغيرات RAM
- **CORS / fetch لا يصل للشيت:** `fetch(...,{mode:'no-cors'})` لا يعمل مع Apps Script (redirect 302 يكسره). الحل: JSONP لكل الطلبات (قراءة وكتابة) عبر `<script>` tag ديناميكي
- **`e.parameter` undefined في GAS:** أضف دائماً `var p = (e && e.parameter) ? e.parameter : {};`
- **SyntaxError يوقف كل JS:** حرف `←` (U+2190) داخل `/* comment */` في JS يكسر بعض المتصفحات — تجنب اليونيكود الخاص حتى داخل التعليقات
- **`selectRole is not defined` / أي "X is not defined":** هذا دائماً عرض جانبي لخطأ syntax سابق (مثل قوس مفقود) أوقف تحميل كل الـ script — ابحث عن SyntaxError الحقيقي أولاً بفحص توازن الأقواس `(` `)`
- **التاريخ يرجع بصيغة طويلة من الشيت:** `Date` objects في Google Sheets تُقرأ كـ `Thu Jan 08 2026 00:00:00 GMT+0300...` — لازم تحويل لـ `YYYY-MM-DD` في كل من GAS و JS
- **الأعمدة الناقصة في الشيتات القديمة:** بدل الافتراض أن الأعمدة موجودة، اقرأ الرأس ديناميكياً بـ `getColMap()` وأنشئ الأعمدة الناقصة تلقائياً بدالة صيانة منفصلة تُشغَّل يدوياً

## نمط العمل المتبع مع المستخدم
- المستخدم (إبراهيم) قليل الصبر مع التفاصيل التقنية — يرسل رسائل خطأ من console مباشرة أو screenshots
- يفضّل حلول فورية وتطبيق مباشر بدون شرح طويل قبل التنفيذ
- بعد كل تعديل: تحقق من توازن الأقواس في الـ JS قبل تسليم الملف (`js.count('(') == js.count(')')`)
- عند تحديث `apexcare_calendar.gs`: يجب في كل مرة تذكيره أن يعيد النشر بـ **Deploy → Manage deployments → Edit → New version → Deploy** (وليس تعديل الكود فقط دون إعادة نشر) — هذا أكثر سبب متكرر لعدم ظهور أي إصلاح
- المستخدم يستخدم الشيت مباشرة أحياناً (يرسل screenshots من Google Sheets) — تحقق دائماً من التطابق بين ما هو متوقع في الكود وما هو فعلي في الشيت

## آخر نقطة توقفنا عندها (لم تُكمَل بعد)
كان جارياً تصميم تفاعل "الضغط على يوم في التقويم" ليعرض قائمة خيارات بدل الانتقال المباشر:
1. عرض أحداث اليوم المُختار (قائمة) → الضغط على حدث يفتح تفاصيله (عنوان/نص/تعليقات) داخل نفس الصفحة
2. إضافة ملاحظة جديدة لهذا اليوم
3. مشاهدة آخر رد على آخر ملاحظة لنفس اليوم

الدالة الحالية `cellTap(td)` بسيطة (تفتح مباشرة اليوم أو تفتح إضافة ملاحظة) — تحتاج إعادة بناء كقائمة خيارات (action sheet / bottom menu) بدل السلوك الحالي.

## أرقام مهمة
- Deployment URL الحالي لـ Apps Script: مُخزَّن في `index.html` كـ `const GAS_URL = '...'`
- Repo GitHub: `apexa5521-hue/Calendar`
